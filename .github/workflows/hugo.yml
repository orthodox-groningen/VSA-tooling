name: Hugo legacy workflow

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest

    steps:
      - name: Notice
        run: |
          echo "This legacy workflow is disabled by default."
          echo "Use hugo-demo.yml for the active Hugo demo build."
