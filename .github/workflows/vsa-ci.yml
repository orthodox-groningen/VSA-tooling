# VSA CI — Windows-validatie bij elke push/PR.
# Doel: scripts\ci.cmd (pytest, validate + build-markdown op examples/consumer-minimal).
# catalogus komt uit orthodox-groningen/bron (niet de PyPI-naamgenoot). Lokaal: scripts\ci.cmd

name: VSA CI

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: vsa-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  windows-ci:
    name: Windows tool CI
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout bron repository
        uses: actions/checkout@v4
        with:
          repository: orthodox-groningen/bron
          path: vendor/bron
          ref: main

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements*.txt
            pyproject.toml

      - name: Run VSA CI
        shell: cmd
        run: scripts\ci.cmd
