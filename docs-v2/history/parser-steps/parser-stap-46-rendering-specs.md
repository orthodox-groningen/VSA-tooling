# Stap 46 - rendering specs uitgewerkt

Deze stap vult drie rendering-specificaties inhoudelijk uit:

- `docs/spec/vsa-layout-algorithm.md`
- `docs/spec/vsa-svg-dom-structure.md`
- `docs/spec/vsa-rendering-config-model.md`

Daarnaast is een test toegevoegd:

```cmd
python -m pytest tests\test_rendering_specs.py -v
```

Doel:

- renderer-revisie voorbereiden;
- layoutalgoritme vastleggen;
- SVG-DOM-structuur vastleggen;
- configmodel vastleggen.
