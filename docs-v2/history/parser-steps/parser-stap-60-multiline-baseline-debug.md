# Stap 60 - multiline baseline debug

Inspecteer de echte `translate(x,y)` waarden van `vsa-line`
groepen in gegenereerde SVG's.

Gebruik:

```cmd
python scripts\inspect-svg-line-y.py ^
examples\hugo-demo\public\vsa\voorbeelden-praktijk-tropaar-toon-8-block-1.svg
```
