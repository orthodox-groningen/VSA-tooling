# Indeling fase 2

Voorlopige indeling op basis van fase 1.

- `getting-started/`: 0 bestaande documenten als directe bron
- `guides/`: 5 bestaande documenten als directe bron
- `specification/`: 2 bestaande documenten als directe bron
- `architecture/`: 6 bestaande documenten als directe bron
- `history/`: 182 bestaande documenten als directe bron
- `reference/`: 29 bestaande documenten als directe bron

## Startvolgorde

1. `guides/` en `specification/` eerst, omdat die de actuele waarheid voor gebruikers vastleggen.
2. Daarna `architecture/`, met compacte actuele ontwerpdocumenten.
3. Daarna `history/`, waarin de parser-stappen vrijwel één-op-één bewaard blijven.
4. Daarna `reference/`, voor TODO’s, prompts, domeinnotities en overige ondersteunende documenten.
