# Stap 43 - policy stabilization

Nieuwe stabiele policy:

```text
[:] == geldige neutrale afsluiting
[-:] == expliciete neutrale afsluiting
```

Dus:

- lege final pitchmarker is toegestaan;
- ontbrekende final pitchmarker blijft fout;
- oude tests zijn gemigreerd naar de nieuwe policy.
