[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vsa-tool"
version = "0.1.0"
description = "VSA parser, validator, renderer and exporter"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
